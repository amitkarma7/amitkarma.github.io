import { Routes } from '@angular/router';

// Single-page portfolio: all sections live on the AppComponent template
// and are navigated to via anchor links / smooth scroll. Routes are kept
// here so the router is wired up and ready if additional pages are added.
export const routes: Routes = [];
